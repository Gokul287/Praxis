# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info/
dist/
build/
.eggs/
.env
.venv
venv/
env/
.venv_wsl

# Testing
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# OpenEnv runtime outputs (never commit)
outputs/
outputs/logs/
outputs/evals/

# IDE (JetBrains-only; we keep `idea/` as the planning folder)
.vscode/
.idea/
*.sw[po]

# Local-only assets that must NOT be pushed
# - idea/OpenEnv: full OpenEnv reference clone, only used as a local read-only
#   pattern source. The team pulls OpenEnv from upstream, not from this repo.
idea/OpenEnv/
FlawsToProduction/

# Local script we use to install Docker in WSL; not part of the env image.
get-docker.sh

# Local research scaffold inside idea/. Only `idea/Plan/` is the live spec
# the team needs to close the 21 issues; everything else is private notes.
idea/agents/
idea/Architecture/
idea/Data/
idea/PROBLEM STATEMENT/
idea/Pre Validation Script
idea/Sample Inference Script
idea/.gitignore
idea/.gitkeep
idea/README.md
idea/Task.md

# Inside idea/Plan/: skip the legacy T01-T14 tracker (superseded by
# github_issues.md) and any Cursor `.plan.md` artefacts.
idea/Plan/task.md
idea/Plan/*.plan.md
.cursor/plans/

# OS
.DS_Store
Thumbs.db

# Environment variables
.env
.env.local
.env.*.local

# Docker
*.tar

# HF
.huggingface/
